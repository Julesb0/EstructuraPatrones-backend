# Configuración ultra-simple para Render - BACK READY FOR RENDER
server:
  port: ${PORT:8080}

spring:
  application:
    name: entrepreneur-platform
  main:
    banner-mode: off
    lazy-initialization: true
    allow-bean-definition-overriding: true