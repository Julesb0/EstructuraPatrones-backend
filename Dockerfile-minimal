# Dockerfile ultra-simple para Render - SIN ERRORES
FROM eclipse-temurin:21-jre-alpine

# Instalar curl para health checks
RUN apk add --no-cache curl

# Crear usuario no-root
RUN addgroup -g 1001 -S appuser && adduser -S appuser -u 1001

# Establecer directorio de trabajo
WORKDIR /app

# Copiar el JAR (asumiendo que ya está construido)
COPY target/*.jar app.jar

# Copiar configuración minimal
COPY src/main/resources/application-render-minimal.yml application.yml

# Cambiar propietario
RUN chown -R appuser:appuser /app

# Cambiar al usuario no-root
USER appuser

# Exponer puerto
EXPOSE 8080

# Establecer perfil activo
ENV SPRING_PROFILES_ACTIVE=render-minimal

# Comando de inicio
CMD ["java", "-jar", "app.jar"]